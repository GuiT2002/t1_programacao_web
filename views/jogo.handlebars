<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="utf-8">
    <title>Partida – Cara-a-Cara</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
</head>

<body data-page="jogo" data-user="{{usuario}}">
    <div class="container-fluid py-3">

        <h4 class="mb-3">Partida em andamento – {{usuario}}</h4>

        <!-- Tabuleiro -->
        <div id="board" class="mb-4"></div>

        <!-- Área de interação -->
        <div class="row g-3 mb-3">
            <div class="col-12 col-lg-6">
                <form id="formPergunta" class="input-group">
                    <input id="txtPergunta" class="form-control" placeholder="Digite sua pergunta (SIM/NÃO)">
                    <button class="btn btn-primary" type="submit">Perguntar</button>
                </form>
            </div>
            <div class="col-12 col-lg-6 d-flex gap-2">
                <button id="btnSIM"  class="btn btn-success flex-fill" disabled>SIM</button>
                <button id="btnNAO"  class="btn btn-danger  flex-fill" disabled>NÃO</button>
            </div>
        </div>

        <!-- Palpite final -->
        <div class="input-group mb-3" style="max-width:300px">
            <label class="input-group-text">Seu palpite</label>
            <select id="selFace" class="form-select"></select>
            <button id="btnGuess" class="btn btn-warning">Chutar!</button>
        </div>

        <!-- Log -->
        <textarea id="log" class="form-control" rows="6" readonly></textarea>
    </div>

    <script src="/app.js"></script>
</body>
</html>
